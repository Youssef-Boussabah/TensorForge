"""tensorforge.backends — experimental compiled backends.

Nothing here is used by the main framework. These namespaces exist to
experiment with calling compiled kernels from Python; importing
tensorforge itself never touches them.
"""
